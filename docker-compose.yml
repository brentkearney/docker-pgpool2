version: '2'

services:
  pgpool:
    build: .
    ports:
      - '8000:80'
      - '9999:9999'
    networks:
      - pgpool
    volumes:
      - ./logs:/var/log/pgpool2
    env_file:
      - '.environment'

networks:
  pgpool:

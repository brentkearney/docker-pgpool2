version: '2'

services:
  pgp:
    build: .
    ports:
      - '8000:80'
      - '9999:9999'
    networks:
      - pgpool
    volumes:
      - ./logs:/var/log/pgpool
    env_file:
      - '.environment'

networks:
  pgpool:

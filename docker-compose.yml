version: '2'

services:
  pgp:
    build: .
    ports:
      - '8000:80'
      - '9999:9999'
    networks:
      - pgpool
    volumes:
      - ./logs:/var/log/pgpool
      - ./config/pgmgt.conf.php:/var/www/html/admin-tool/conf/pgmgt.conf.php
      - ./config/etc:/usr/local/etc
    env_file:
      - '.environment'

networks:
  pgpool:

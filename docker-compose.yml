version: '2'

services:
  pgpool:
    build: .
    ports:
      - '8000:80'
      - '9999:9999'
    networks:
      - pgpool
    volumes:
      - ./config:/usr/local/etc
    env_file:
      - '.environment'

networks:
  pgpool:
